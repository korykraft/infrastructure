---
searxng_dir: "{{ docker_root }}/searxng"
# Port is defined in group_vars/all/vars.yml:
#   searxng_port: 8080

searxng_base_url: "http://{{ ansible_default_ipv4.address }}:{{ searxng_port }}/"
searxng_secret_key: "changeme"
